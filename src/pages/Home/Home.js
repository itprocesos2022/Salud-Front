import { Box } from '@material-ui/core'
import { ModuleIndicator } from '../../components/Shared'
import { PageHeading } from '../../components/UI'

const Home = () => (
  <Box>
    <ModuleIndicator module={'HOME'} />
    <PageHeading>home en construcción</PageHeading>
  </Box>
)

export default Home
